# Copy to .env only if you need to override the default. DocDesk runs without it.

# In development Vite proxies /api to the Express server, so this is not needed.
# Set it only when the frontend is deployed separately from the API.
# VITE_API_URL=https://your-api.example/api
